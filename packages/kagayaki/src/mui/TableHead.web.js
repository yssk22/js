/* @flow */
import React from 'react';
import TableHead from '@material-ui/core/TableHead/TableHead';
import type { TableHeadProps } from './TableHead.common';

const _TableHead = function(props: TableHeadProps) {
  return <TableHead {...props} />;
};

export default _TableHead;
