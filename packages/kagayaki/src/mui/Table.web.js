/* @flow */
import React from 'react';
import Table from '@material-ui/core/Table/Table';
import type { TableProps } from './Table.common';

const _Table = function(props: TableProps) {
  return <Table {...props} />;
};

export default _Table;
